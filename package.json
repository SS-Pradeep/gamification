{
  "name": "gamification-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "author": "adityabmv, jeyadevan",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0"
  },
  "scripts": {
    "prepare": "husky",
    "binaries": "pnpx ts-node scripts/preload-mongo-binary.ts"
  },
  "lint-staged": {
    "backend/**/*.{js,jsx,ts,tsx}": [
      "pnpm --filter backend lint"
    ]
  }
}
